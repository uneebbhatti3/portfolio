import Image from "next/image";

export default function PostgreSQLIcon() {
  return (
    <Image
      src={"/svg/postgresql.svg"}
      alt="PostgreSQL"
      width={20}
      height={20}
    />
  );
}
